# TenJudge-Server

`tenjudge-server` 是 **TenJudge 在线评测系统（OJ）** 的服务端。

## 模块设计
- Auth：用户认证模块，负责用户登录、注册、权限管理等功能。
- Problem：题目管理模块，负责题目的创建、编辑、删除、查询等功能。
- Contest：比赛管理模块，负责比赛的创建、编辑、删除、查询等功能。
- Submission：提交管理模块，负责用户提交的代码的存储、编译


## 部署
- 目录挂载：统一仅挂载一个目录，不应将 `data` 与 `temp` 目录分开挂载，提高性能，并保证部分操作的原子性
- 环境变量：