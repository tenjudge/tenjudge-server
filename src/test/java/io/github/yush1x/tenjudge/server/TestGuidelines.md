# 测试规范

- 请仅根据功能描述、输入输出契约和业务场景设计测试用例，禁止基于已有实现逻辑推导测试用例

## 单元测试生成要求

- 验证业务规则，不验证实现细节（不关心内部调用顺序/次数）
- 命名：`方法名_业务场景`
- 业务规则以文档为准，而非代码实现

### Mock
- 只 mock 直接依赖，不 mock MyBatis/Redis/Sa-Token 等底层库
- mock 行为要覆盖：正常 / 空值 / 异常 / 边界
- 少用 verify，仅用于关键副作用

### 用例覆盖
- 正常场景
- 关键分支
- 边界值
- 空值/null
- 非法输入或状态
